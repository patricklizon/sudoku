import type { Grid, GridFilled } from './grid';
import type { EncodedPuzzle } from './types';

/**
 * Encodes a solvable puzzle into a string format
 */
export function encodePuzzle(_puzzle: Grid, _solution: GridFilled): EncodedPuzzle {
	return '' as EncodedPuzzle;
}
