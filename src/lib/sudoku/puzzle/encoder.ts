import type { EncodedPuzzleSolvable, PuzzleSolvable } from './types';

/**
 * Encodes a solvable puzzle into a string format
 */
export function encodePuzzle(g: PuzzleSolvable): EncodedPuzzleSolvable {
	return '' as EncodedPuzzleSolvable;
}
