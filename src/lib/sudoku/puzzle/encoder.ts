import type { EncodedPuzzle, PuzzleSolvable } from './types';

/**
 * Encodes a solvable puzzle into a string format
 */
export function encodePuzzle(g: PuzzleSolvable): EncodedPuzzle {
	return '' as EncodedPuzzle;
}
