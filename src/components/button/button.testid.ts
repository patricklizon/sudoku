/**
 * Defines data-testid attributes for the Button component and its internal elements.
 * Use these constants to ensure consistency between component implementation and tests.
 */
export const ButtonTestId = {
	/**
	 * Use this to select the button component itself in tests.
	 */
	root: "button-component-root",
};
